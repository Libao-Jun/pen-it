---
name: utility-func
description: 工具函数全流程 —— 生成/优化工具函数 → 标准注释 → 测试用例 → 跑测修bug → 更新README
---

# 工具函数全流程

本 skill 用于为 `pen-it` 项目新增一个工具函数模块，覆盖从代码编写到文档更新的完整流程。

## 触发条件

当用户请求：
- 新增/添加工具方法/函数
- 新增函数/方法
- 补充函数/方法
- 在 `src/` 下新建模块

的时候触发

## 项目约定速查

| 规则     | 说明                                                                         |
| -------- | ---------------------------------------------------------------------------- |
| 模块目录 | `src/<module>/index.ts`                                                      |
| 测试目录 | `tests/<module>.test.ts`                                                     |
| 入口导出 | `src/index.ts` 中添加 `export * from './<module>'`                           |
| 测试框架 | vitest，使用 `describe` / `it` / `expect`                                    |
| 类型检查 | `npx tsc --noEmit`                                                           |
| 运行测试 | `npx vitest run`（全量）或 `npx vitest run tests/<module>.test.ts`（单模块） |
| 注释风格 | JSDoc，中文简述，含 `@param` / `@returns`精简不冗余           |

## 参考现有代码

- 源码风格参考 → `src/array/index.ts`
- 测试风格参考 → `tests/array.test.ts`
- README API 文档格式参考 → `README.md` 中 `### 数组 — \`array\`` 段落
- 更新日志文档放入 CHANGELOG.md  中，后续更新日志参考现有的文档格式

## 流程步骤

按顺序执行以下 7 步，每步完成后确认无误再进入下一步。

### 第 0 步：检测是否已存在新增工具函数

**目的**：避免重复造轮子——在编写任何代码之前，先确认待新增的函数是否已存在于项目中。

**操作**：

1. **按函数名精确搜索**：在 `src/` 目录下搜索与待新增函数同名的导出
2. **按功能模糊搜索**：用功能关键词（如"脱敏""校验""格式化""判断"）在 `src/` 中搜索已有实现
3. **按模块巡检**：若用户指定了模块，通读该模块的 `src/<module>.ts`，确认无功能重叠
4. **交叉比对**：若搜索命中，对比已有函数与待新增函数的：
   - **功能**：是否解决同一问题？
   - **签名**：入参/出参是否一致或可互相替代？
   - **实现**：核心算法是否相同？

**决策分支**：

| 情况 | 处理方式 |
|------|----------|
| 完全重复（同名 + 同功能） | 告知用户已存在，终止新增流程 |
| 功能高度重叠（不同名但同质） | 列出已有函数，询问用户是复用、扩展还是替换 |
| 部分重叠（可合并） | 建议扩展已有函数（增加可选参数），而非新增 |
| 无冲突 | 继续第 1 步 |

**自查清单**：

- 是否搜索了函数名和功能关键词？
- 是否通读了目标模块的现有代码？
- 发现冲突时是否明确告知用户并等待决策？
- 确认无冲突后才进入第 1 步？

### 第 1 步：生成工具函数

**输入**：用户描述的需求或函数名。

**操作**：

1. 确定函数所属模块（复用已有模块或新建模块）
2. 在 `src/<module>/index.ts` 中编写实现
3. 函数要求：
   - **纯函数**优先——不修改入参，不依赖外部状态
   - 类型安全——合理的 TypeScript 泛型约束
   - 精简——功能内聚，不过度抽象

**自查清单**：

- 是否遵循单向数据流（输入 → 输出）？
- 是否避免了不必要的副作用？
- TypeScript 类型是否推导正确？
- 边界情况是否处理（空值、极端值、异常输入）？

### 第 2 步：添加标准注释

**规则**：

1. 模块顶部用 `//` 写中文分类标题
2. 每个导出函数上方写 JSDoc：
   - 第一行：`/**` 后跟一句中文功能描述
   - `@param` — 参数说明（含默认值）
   - `@returns` — 返回值说明
   - `@example` — 一个精简用例（最多一个，控制台输出用 `// =>` 标注）
3. **禁止**：多段落叙述、场景罗列、实现原理注释、行内注释、类比比喻、`@example` 内写 fetch/addEventListener 等完整场景代码
4. 参考 `src/array/index.ts` 中的注释密度

### 第 3 步：生成测试用例

**在 `tests/<module>.test.ts` 中生成测试**：

1. 引入 vitest：`import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'`
2. 引入被测函数：`import { fn1, fn2 } from '../src/<module>'`
3. 用 `// ====` 分隔各函数的测试区块
4. 每个函数覆盖：
   - 基本功能（happy path）
   - 边界情况（空值、单元素、极端值）
   - 不修改原始数据（若为纯函数）
   - 各参数组合 / 分支路径
5. 若涉及定时器，使用 `vi.useFakeTimers()` + `vi.advanceTimersByTime()`

### 第 4 步：跑测试并修 bug

**操作**：

1. 先跑类型检查：`npx tsc --noEmit`
2. 再跑测试：`npx vitest run tests/<module>.test.ts`
3. 若失败，根据报错定位源码或测试的 bug
4. 修复后回到步骤 2（检查注释是否需要同步调整）
5. 确保全部通过后再跑全量：`npx vitest run`

**常见 bug 排查**：

- 测试期望写错（时序、调用次数）
- `lastArgs` 在闭包中未及时更新
- 边界条件（delay=0、空参数、连续快速调用）
- 状态残留（`beforeEach` 未正确重置）

### 第 5 步：更新 README API 文档

**在 `README.md` 中添加新模块的 API 段落**：

格式模板（严格遵循现有格式）：

````markdown
### 模块中文名 — `module`

- **`fnName(param)`** — 一句话功能描述。

```ts
fnName(arg);
// => result
```
````

**规则**：

- 用 `###` 标题，格式：`### 中文名 — \`module\``
- 每个函数一行：`- **\`fnName(param)\`\*\* — 描述。`
- 紧跟一个 ` ```ts ` 代码块展示用法和输出
- 若模块函数较多，可按子分类拆分为多个 `###` 标题（参考 `format` 模块）
- 新模块的段落插入到 `---` 分隔线之前，与已有模块保持一个空行间距

### 第 6 步：更新 CHANGELOG.md 文档

- 文档格式和规范参照现有的 `CHANGELOG.md` 更新日志文档规范

## 完成标志

全部 7 步执行完毕，`npx tsc --noEmit` 零错误，`npx vitest run` 全量通过，README 已更新，CHANGELOG 已更新。

## 总结-不参与整个流程

> 总结一下创建的内容，该总结仅仅作为一个目录使用，不参与整个流程

```txt
┌─────────┬─────────────────────────────────────────────────────────────┐
│  步骤   │                            内容                              │
├─────────┼─────────────────────────────────────────────────────────────┤
│ 第 0 步 │ 检测重复 — 搜索已有函数，对比功能/签名/实现，避免重复造轮子       │
├─────────┼─────────────────────────────────────────────────────────────┤
│ 第 1 步 │ 生成工具函数 — 纯函数、类型安全、边界处理                       │
├─────────┼─────────────────────────────────────────────────────────────┤
│ 第 2 步 │ 添加标准注释 — JSDoc 精简格式，参考 src/array/index.ts         │
├─────────┼─────────────────────────────────────────────────────────────┤
│ 第 3 步 │ 生成测试用例 — vitest，覆盖基本/边界/不修改原数据/参数组合       │
├─────────┼─────────────────────────────────────────────────────────────┤
│ 第 4 步 │ 跑测试修 bug — 先 tsc --noEmit，再 vitest run，失败定位修复    │
├─────────┼─────────────────────────────────────────────────────────────┤
│ 第 5 步 │ 更新 README — 按现有 API 文档格式补充新模块                    │
├─────────┼─────────────────────────────────────────────────────────────┤
│ 第 6 步 │ 更新 CHANGELOG — 按现有 CHANGELOG.md 文档格式补充新模块        │
└─────────┴─────────────────────────────────────────────────────────────┘
```
